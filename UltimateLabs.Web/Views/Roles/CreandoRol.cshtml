
@model UltimateLabs.Web.Models.RolesViewModel
@Scripts.Render("~/bundles/jqueryval")
@Styles.Render("~/ThemeAdmin/css")
@{
    ViewBag.Title = "CreandoRol";
    Layout = "~/Views/Shared/_LayoutTry.cshtml";
}

<head>
    <link href="https://fonts.googleapis.com/css?family=Charmonman|Roboto" rel="stylesheet">
    @Styles.Render("~/Content/CSS/Styles.css")
    @Styles.Render("~/Content/basic.css")
    @Styles.Render("~/Content/dropzone.css")
    @Styles.Render("~/Content/Register.css")
    <script src="~/Scripts/jquery.filedrop.js"></script>
    <link rel="stylesheet" href="~/Content/basic.css" />
    <link rel="stylesheet" href="~/Content/dropzone.css" />
    <script src="~/Scripts/dropzone.js"></script>
</head>


@using (Html.BeginForm("CreandoRol", "Roles", FormMethod.Post, new { @id = "CreandoRol", enctype = "multipart/form-data" }))
{

    @Html.AntiForgeryToken()
    <h6 style="color:#02779d;margin-left:3%;font-family:'Roboto',sans-serif">Seguridad > Crear Roles</h6>
    <h3 style="color:#02779d;font-family:'Roboto',sans-serif" class="text-center">Crear Roles</h3>

    <div class="col-sm-12 contenedor">
        <h4>Create a new role.</h4>
        <hr />
        @Html.ValidationSummary("", new { @class = "text-danger" })
        <div class="form-group row">
            @Html.LabelFor(m => m.Name, new { @class = "col-md-3 control-label" })
            <div class="col-md-9">
                @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" class="btn btn-default" value="Create Role" />
            </div>
        </div>

    </div>
}
